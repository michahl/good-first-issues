const Logo = ({ size }) => {
  return (
    <div>
        <h1 
            className={`${size} font-logo font-bold select-none`}
        >
            BugScout
        </h1>
    </div>
  )
}

export default Logo